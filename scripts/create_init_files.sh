#!/bin/bash

# This script previously generated __init__.py scaffolding but the project now
# ships curated package modules under src/endo_seg. The generation logic is
# intentionally disabled to avoid overwriting those files.

echo "create_init_files.sh is no longer required. All package initialisers are tracked in git."
echo "No files were modified."
